Make sure to add the .pth for the nnunet 
add the best_checkpoint.pth, download from google drive in
backend -> models -> trained_weights -> nnunet -> nnunet_results_ct_pet -> fold_0 
add to the directory above
